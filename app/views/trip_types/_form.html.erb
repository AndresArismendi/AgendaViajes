<%= form_for(trip_type) do |f| %>
  <% if trip_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trip_type.errors.count, "error") %> prohibited this trip_type from being saved:</h2>

      <ul>
      <% trip_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
