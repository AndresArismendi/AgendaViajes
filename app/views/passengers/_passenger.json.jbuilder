json.extract! passenger, :id, :nombre, :telefono, :email, :cantidad, :hostal, :documento, :idioma, :booking, :estado_disponibilidad, :created_at, :updated_at
json.url passenger_url(passenger, format: :json)
